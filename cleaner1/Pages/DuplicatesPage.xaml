<Page x:Class="ModernFileCleaner.Pages.DuplicatesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Duplicates">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE8B7;" FontFamily="/Wpf.Ui;Component/Resources/Font/#Segoe Fluent Icons" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel>
                <TextBlock Text="Duplicate File Finder" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock Text="Find and remove duplicate files" FontSize="13" Foreground="#FF888888"/>
            </StackPanel>
        </StackPanel>

        <!-- Path Picker -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12" HorizontalAlignment="Center">
            <TextBlock Text="Scan location:" Foreground="#FF888888" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="cmbPath" Width="200" Margin="0,0,10,0"
                      SelectionChanged="cmbPath_SelectionChanged">
                <ComboBoxItem Content="Desktop" Tag="desktop"/>
                <ComboBoxItem Content="Documents" Tag="documents"/>
                <ComboBoxItem Content="Downloads" Tag="downloads"/>
                <ComboBoxItem Content="Custom..." Tag="custom"/>
            </ComboBox>
            <TextBox x:Name="txtCustomPath" Width="250" Visibility="Collapsed"
                     Text="C:\Users\Azubi\Documents" />
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16" HorizontalAlignment="Center">
            <ui:Button x:Name="btnScan"
                       Content="&#x1F50D;  Scan for Duplicates"
                       Appearance="Primary"
                       Width="200" Height="38"
                       Click="btnScan_Click"/>
            <ui:Button x:Name="btnDelete"
                       Content="&#x1F5D1;  Delete Selected"
                       Appearance="Danger"
                       Width="160" Height="38"
                       Margin="10,0,0,0"
                       IsEnabled="False"
                       Click="btnDelete_Click"/>
        </StackPanel>

        <!-- Results List -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
            <ItemsControl x:Name="resultsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1" Padding="4" Margin="0,0,0,4">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Hash}" FontFamily="Consolas" FontSize="11" Foreground="#FF888888" Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding SizeFormatted}" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="{Binding Files.Count, StringFormat=' ({0} files)'}" Foreground="#FF888888" Margin="4,0,0,0"/>
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding Files}" Margin="28,4,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontSize="11" Foreground="#FFCCCCCC" TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Progress Bar -->
        <ProgressBar x:Name="ProgressBar"
                     Grid.Row="4"
                     Style="{StaticResource SmoothProgressBar}"
                     Margin="0,0,0,8"
                     Visibility="Collapsed"
                     IsIndeterminate="True"/>

        <!-- Status Text -->
        <TextBlock x:Name="txtStatus"
                   Grid.Row="5"
                   Text="Ready"
                   Foreground="#FF888888"
                   HorizontalAlignment="Center"
                   FontSize="13"/>
    </Grid>
</Page>
