<Page x:Class="ModernFileCleaner.Pages.StartupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:services="clr-namespace:ModernFileCleaner.Services"
      Title="Startup Manager">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE8FD;" FontFamily="/Wpf.Ui;Component/Resources/Font/#Segoe Fluent Icons" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel>
                <TextBlock Text="Startup Manager" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock Text="Manage programs that run at startup" FontSize="13" Foreground="#FF888888"/>
            </StackPanel>
        </StackPanel>

        <!-- Startup Items List -->
        <ListView x:Name="StartupListView" Grid.Row="1" Margin="0,0,0,16"
                  Background="Transparent"
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type services:StartupItem}">
                    <Border CornerRadius="8"
                            Background="#FF2D2D2D"
                            BorderBrush="#FF3D3D3D"
                            BorderThickness="1"
                            Padding="12"
                            Margin="0,0,0,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Name and Source -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="14" FontWeight="SemiBold"
                                           Foreground="White"
                                           VerticalAlignment="Center"/>
                                <Border Background="#FF3A3A3A"
                                        CornerRadius="4"
                                        Padding="6,2"
                                        Margin="8,0,0,0">
                                    <TextBlock Text="{Binding Source}"
                                               FontSize="11"
                                               Foreground="#FF888888"/>
                                </Border>
                            </StackPanel>

                            <!-- Command -->
                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="{Binding Command}"
                                       FontSize="12"
                                       Foreground="#FF888888"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,4,0,0"/>

                            <!-- Toggle Switch -->
                            <ui:ToggleSwitch Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                                             IsChecked="{Binding Enabled, Mode=OneWay}"
                                             VerticalAlignment="Center"
                                             Margin="12,0,0,0"
                                             Click="ToggleSwitch_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <ui:Button x:Name="btnRefresh"
                       Content="&#xE72C;  Refresh"
                       Appearance="Primary"
                       Width="140" Height="38"
                       Click="Refresh_Click"/>
        </StackPanel>
    </Grid>
</Page>
